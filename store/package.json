{
  "name": "acsilserver-store",
  "version": "0.1.0",
  "description": "Acsilserver Plugins Store",
  "homepage": "http://store.acsilserver.com/",
  "private": true,
  "devDependencies": {
    "bower": "^1.3.12",
    "grunt": "^0.4.5",
    "http-server": "^0.7.3"
  },
  "scripts": {
    "postinstall": "bower install",
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"
  }
}
